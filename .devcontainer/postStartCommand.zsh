#!/bin/zsh

set -ex

# required to source this
source "${NXE_SCRIPTS}/init-postStart-base.sh" && echo "Sourced ${NXE_SCRIPTS}/init-postStart-base.sh"
#source $NXE_WS/.devcontainer/testStart.zsh


cat <<EndOfMessage

                                          HAPPY HACKING!

EndOfMessage
#source .zsh_config
